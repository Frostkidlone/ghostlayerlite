[package]
name = "ghostlayer"
version = "0.1.0"
edition = "2021"

[dependencies]
ink = { version = "4.2.0", default-features = false }

[lib]
name = "ghostlayer"
path = "lib.rs"
crate-type = ["cdylib"]

[features]
default = ["std"]
std = ["ink/std"]
